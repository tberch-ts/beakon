{
  "name": "beakon-functions",
  "version": "0.1.0",
  "description": "Beakon Cloud Functions: API, Stripe webhook, scheduled monitor checks.",
  "type": "module",
  "main": "index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "serve": "firebase emulators:start --only functions,firestore,auth,hosting",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "express": "^4.19.2",
    "firebase-admin": "^12.5.0",
    "firebase-functions": "^5.1.1",
    "nodemailer": "^6.9.14",
    "stripe": "^16.8.0"
  }
}
