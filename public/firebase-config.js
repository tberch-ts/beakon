// public/firebase-config.js
// Paste your Firebase Web app config here (Firebase console → Project settings →
// "Your apps" → SDK setup and configuration → Config). These values are NOT secret;
// they identify your project to the browser. Access is controlled by Firebase Auth
// and Firestore rules, not by hiding this config.
export const firebaseConfig = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_PROJECT.firebaseapp.com",
  projectId: "YOUR_PROJECT_ID",
  appId: "YOUR_APP_ID"
};
